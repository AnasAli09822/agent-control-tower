# AI usage

AI assistance was used for implementation planning, code generation, test design, and review. System behavior is not presented as proof merely because generated code exists: database state, event evidence, adversarial tests, public build behavior, and explicit limitations are the acceptance criteria.

The demo may use deterministic simulated reasoning/tool steps. When it does, usage entries are labeled `simulated`; they are not represented as provider billing records.
